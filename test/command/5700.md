```
% pandoc -t native -s --metadata-file command/5700-metadata-file-1.yml --metadata-file command/5700-metadata-file-2.yml
^D
Pandoc
  Meta
    { unMeta =
        fromList
          [ ( "desc"
            , MetaInlines
                [ Str "Both of these files should be loaded." ]
            )
          , ( "title"
            , MetaInlines [ Str "Multiple metadata files test" ]
            )
          ]
    }
  []
```
