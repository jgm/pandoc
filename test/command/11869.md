A link's title is written to docx as a ScreenTip (`w:tooltip`) and read back.

```
% pandoc -f markdown -t docx -o - | pandoc -f docx -t markdown
See [the report](https://example.org/r.pdf "Annual report, PDF, 2 MB").
^D
See [the report](https://example.org/r.pdf "Annual report, PDF, 2 MB").
```
